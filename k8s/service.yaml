apiVersion: v1
kind: Service
metadata:
  name: ai-k8s-agent
  namespace: ai-k8s-agent
spec:
  type: ClusterIP
  selector:
    app: ai-k8s-agent
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
